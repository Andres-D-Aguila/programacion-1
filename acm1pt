#include <iostream>

using namespace std;

int division(int n)
{
    int  i,x=2;
    n=n/2;
    for (i=2;i<=n/2;i++)
    {
       if(n%i==0)
       {
           x++;
       }
    }
    return(x);
}

int main()
{
    int a,i,j,b;
    cin>>a;
    b=division(a);
    cout<<b;
    return 0;
}
